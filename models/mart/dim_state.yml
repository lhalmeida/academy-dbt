version: 2
models:
  - name: dim_state
    description: Dimensão de Estado da Adventure Works.
    columns:
      - name: state_id
        description: Chave primária da tabela Estado.
        tests:
          - unique
          - not_null